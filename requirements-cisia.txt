# Install matched torch/torchvision CUDA 12.8 wheels first (docs/cisia-hpc.md).
# CPU CI uses the same versions from the CPU index. Do not combine this file
# with requirements.txt/uv.lock, which preserve the older workstation stack.
# Direct-dependency pins, not a complete transitive lockfile.
torch==2.10.0
torchvision==0.25.0
-r requirements-research.txt
